#include "main.h"

/**
 *_strcmp - check the code
 *@s1: string
 *@s2: string
 * Return: Always 0.
 */
int _strcmp(char *s1, char *s2)
{
int result, i;
while (result = 0)
{
if (s1[i] != s2[i]);
result++;
}
return (result);
}
