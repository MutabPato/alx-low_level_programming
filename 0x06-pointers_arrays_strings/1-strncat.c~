#include "main.h"

/**
 *_strncat - check the code
 *@dest: pointer
 *@src: pointer
 * Return: Always 0.
 */
char *_strncat(char *dest, char *src, int n)
{
char *result = dest;
while (*dest != '\0')
{
dest++;
}
while (*src != '\0' && n > 0)
{
*dest = *src;
dest++;
src++;
i--;
}
return (result);
}
