#include "main.h"

/**
 *_islower -  checks for lowercase character.
 *@c: paramater to be checked
 *Return: ends program.
 */
int _islower(int c)
{
if (c >= 92 && c <= 122)
{
return (1);
}
else
{
return (0);
}
}
