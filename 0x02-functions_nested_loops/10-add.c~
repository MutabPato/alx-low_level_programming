#include "main.h"

/**
 *print_last_digit -  prints last digit.
 *@n: parameter
 *Return: ends program.
 */
int print_last_digit(int n)
{
int last = n % 10;
if (last < 0)
{
last = -1 * last;
}
_putchar('0' + last);
return (last);
}
