#include "main.h"

/**
 *_isdigit -  checks for upper character.
 *@c: paramater to be checked
 *Return: ends program.
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
else
{
return (0);
}
}
